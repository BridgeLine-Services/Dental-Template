import { siteConfig } from '@/lib/config'
export default function LocalBusinessSchema() {
  const schema = { "@context": "https://schema.org", "@type": "Dentist", name: siteConfig.name, url: siteConfig.url, telephone: siteConfig.phone, priceRange: "$$", address: { "@type": "PostalAddress", streetAddress: siteConfig.address.street, addressLocality: siteConfig.address.city, addressRegion: siteConfig.address.state, postalCode: siteConfig.address.zip }, geo: { "@type": "GeoCoordinates", latitude: siteConfig.geo.lat, longitude: siteConfig.geo.lng }, openingHoursSpecification: siteConfig.hours.filter(h => h.hours !== 'Emergency only' && h.hours !== 'By appointment').map(h => { const slots: any[] = []; if (h.hours.includes('9:00 AM - 5:00 PM')) slots.push({ opens: '09:00', closes: '17:00' }); else if (h.hours.includes('8:00 AM - 3:00 PM')) slots.push({ opens: '08:00', closes: '15:00' }); else if (h.hours.includes('9:00 AM - 7:00 PM')) slots.push({ opens: '09:00', closes: '19:00' }); return slots.length ? { "@type": "OpeningHoursSpecification", dayOfWeek: h.day, ...slots[0] } : null }).filter(Boolean) }
  return <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(schema) }} />
}
